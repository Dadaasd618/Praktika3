<UserControl x:Class="LaboratoryModule.Views.ProductBatchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="StatusBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="planned">
                    <Setter Property="Background" Value="#94A3B8"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="running">
                    <Setter Property="Background" Value="#3B82F6"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="quality_control">
                    <Setter Property="Background" Value="#F59E0B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="approved">
                    <Setter Property="Background" Value="#10B981"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="blocked">
                    <Setter Property="Background" Value="#EF4444"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="completed">
                    <Setter Property="Background" Value="#10B981"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#3B82F6" CornerRadius="10" Padding="15,12" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🏭 Готовая продукция" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Grid.Column="1" Text="Контроль качества" FontSize="12" Foreground="#BFDBFE" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="10" Padding="12" Margin="0,0,0,15" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchBox" Height="35" Style="{StaticResource ModernTextBox}" 
                         TextChanged="SearchBox_TextChanged" VerticalContentAlignment="Center"/>

                <ComboBox x:Name="StatusFilter" Grid.Column="1" Width="130" Height="35" Margin="10,0,0,0"
                          Style="{StaticResource ModernComboBox}" SelectionChanged="StatusFilter_SelectionChanged">
                    <ComboBoxItem Content="Все статусы" IsSelected="True"/>
                    <ComboBoxItem Content="planned"/>
                    <ComboBoxItem Content="running"/>
                    <ComboBoxItem Content="quality_control"/>
                    <ComboBoxItem Content="approved"/>
                    <ComboBoxItem Content="blocked"/>
                    <ComboBoxItem Content="completed"/>
                </ComboBox>

                <DatePicker x:Name="DateFrom" Grid.Column="2" Width="110" Height="35" Margin="10,0,0,0"
                            SelectedDateChanged="DateFrom_SelectedDateChanged"/>
                <TextBlock Grid.Column="3" Text="—" VerticalAlignment="Center" Margin="8,0" FontSize="14" Foreground="#64748B"/>
                <DatePicker x:Name="DateTo" Grid.Column="4" Width="110" Height="35" Margin="0,0,0,0"
                            SelectedDateChanged="DateTo_SelectedDateChanged"/>
            </Grid>
        </Border>

        <DataGrid x:Name="BatchesGrid" Grid.Row="2" 
                  Style="{StaticResource ModernDataGrid}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="BatchesGrid_SelectionChanged"
                  RowHeight="45"
                  FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер партии" Binding="{Binding BatchNumber}" Width="130" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="200"/>
                <DataGridTextColumn Header="Заказ" Binding="{Binding OrderNumber}" Width="120"/>
                <DataGridTextColumn Header="Кол-во (кг)" Binding="{Binding ActualQuantityKg}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Дата запуска" Binding="{Binding StartTime, StringFormat='dd.MM.yyyy'}" Width="100"/>
                <DataGridTemplateColumn Header="Статус" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StatusBorderStyle}">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" 
                                           FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Действия" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🔬 Испытание" Style="{StaticResource PrimaryButton}" 
                                    Click="CreateTest_Click" Tag="{Binding}" Width="90" Height="30" FontSize="11"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>