<UserControl x:Class="OperatorModule.Views.ActiveBatchesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Width="941">

    <UserControl.Resources>
        <Style x:Key="StatusIndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasWarning}" Value="true">
                    <Setter Property="Background" Value="#F59E0B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding HasCriticalDeviation}" Value="true">
                    <Setter Property="Background" Value="#EF4444"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding HasWarning}" Value="false">
                    <Setter Property="Background" Value="#10B981"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding HasCriticalDeviation}" Value="false">
                    <Setter Property="Background" Value="#10B981"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#3B82F6" CornerRadius="12" Padding="15,12" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Активные партии" FontSize="18" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
                <Border Grid.Column="1" Background="#10B981" CornerRadius="20" Padding="8,4">
                    <TextBlock Text="● LIVE" FontSize="12" Foreground="White" FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="12" Padding="15" Margin="0,0,0,15" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" Height="38" Style="{StaticResource ModernTextBox}" 
                         TextChanged="SearchBox_TextChanged" VerticalContentAlignment="Center" 
                         Tag="Поиск по номеру партии..."/>
                <ComboBox x:Name="LineFilter" Grid.Column="1" Width="120" Height="38" Margin="10,0,0,0"
                          Style="{StaticResource ModernComboBox}" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="Все линии" IsSelected="True"/>
                    <ComboBoxItem Content="L-01"/>
                    <ComboBoxItem Content="L-02"/>
                    <ComboBoxItem Content="L-03"/>
                </ComboBox>
                <ComboBox x:Name="StatusFilter" Grid.Column="2" Width="130" Height="38" Margin="10,0,0,0"
                          Style="{StaticResource ModernComboBox}" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="Все статусы" IsSelected="True"/>
                    <ComboBoxItem Content="running"/>
                    <ComboBoxItem Content="quality_control"/>
                </ComboBox>
                <Button Grid.Column="3" Content="🔄" Width="45" Height="42" Margin="10,0,0,0"
                        Style="{StaticResource SecondaryButton}" Click="Refresh_Click" FontSize="16"/>
            </Grid>
        </Border>

        <DataGrid x:Name="BatchesGrid" Grid.Row="2" 
                  Style="{StaticResource ModernDataGrid}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="BatchesGrid_SelectionChanged"
                  RowHeight="55"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StatusIndicatorStyle}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="№ партии" Binding="{Binding BatchNumber}" Width="120" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="180"/>
                <DataGridTextColumn Header="Линия" Binding="{Binding Line}" Width="80"/>
                <DataGridTextColumn Header="Текущий шаг" Binding="{Binding CurrentStep}" Width="160"/>
                <DataGridTemplateColumn Header="Статус партии" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="8,4" 
                                    Background="#3B82F6" Width="85" Height="28" HorizontalAlignment="Center">
                                <TextBlock Text="В работе" Foreground="White" FontSize="11" 
                                           FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Статус шага" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="8,4" 
                                    Background="#F59E0B" Width="85" Height="28" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding CurrentStepStatus}" Foreground="White" FontSize="11" 
                                           FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Действия" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="▶" ToolTip="Перейти к программе" 
                    Style="{StaticResource PrimaryButton}" 
                    Click="GoToProgram_Click" Tag="{Binding}" 
                    Width="50" Height="45" FontSize="14"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>